import React from 'react';
import { Button } from 'antd';
import 'antd/dist/antd.css';

const Navbar = () => {
  return (
    <nav>
      <Button>Home</Button>
    </nav>
  );
};

export default Navbar;
